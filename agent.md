# agent.md — AI 工作规范手册

> 用途：规范 AI 在本项目中的行为。新会话开始任务前，先读本文件。
> 本文件**只写规范，不做归档**——各章复检存档、当前任务状态在 `docs/00-模板/AI复检归档.md`（2026-09-02 用户确立分工）。
> 最近更新：2026-09-02（规范/归档分离；概念卡片进入主线章节；事实核对补"逐驱动核实"条款）

---

## 0. 会话启动协议（用户与本 AI 共同遵守，2026-08-18 确立）

**核心原则：状态进文件（无损），会话随便换（有损无所谓）。**

- **写作/返工类任务必须开新对话**：一章或 2~3 篇一个新会话，批量越大后期质量越差（B 扩展赶工教训：单会话大批量推进导致 44 篇偏薄返工）。
- **新会话第一句话由用户点名本文件**：例如"先读 agent.md 和返工清单，本次只写 X、Y 两篇，写完跑行数核对再汇报"——恢复标准、限定批量、声明验收闸口，一句话三件事。
- **复检/修改类任务可续用当前会话**：标准每次都从 plan.md 现读，对上下文长度不敏感。
- **换会话信号**：AI 回复开始变短、汇报表格变简略，即为上下文质量下降，用户应叫停并开新对话。
- **用户无需手动压缩上下文**：压缩由系统自动做且有损，手工无法干预质量；所有关键状态必须落在本文件与 `docs/00-模板/` 下。

---

## 1. 项目一句话

《嵌入式 Linux 知识体系 V2026》：38 章主线（五部）+ A/B/C/D… 扩展篇的丛书工程，源码为 MkDocs Markdown，站点构建用 `mkdocs`。

## 2. 关键路径

| 内容 | 路径 |
|---|---|
| 正文 | `docs/0X-…/第N章 …/X.Y.Z_标题.md` |
| 全书状态总表 | `docs/index.md`（每章一行，含「是否人工复检」列） |
| 风格规范（22 条） | `plan.md`（R1–R22，原为第 4 章制定，实际全书通用） |
| 扩展篇设计 | `docs/00-模板/扩展篇设计大纲_v1.md` |
| 复检归档与工作状态 | `docs/00-模板/AI复检归档.md` |
| 内核源码（核对基准） | `help-docs/kernel-src-v6.6/` |
| 配图生成脚本 | `tools/gen_ch11_diagrams.py`（第 11 章 9 张图） |

## 3. AI 复检流程

**章级前置（每章复检开始前必做，2026-08-25 用户确立，不可或缺）**：先对照本 agent.md 与《help-docs/嵌入式Linux知识体系V2026_最终版_38章.docx》做整章框架调研与设计——大纲对该章的设计意图、当前实际结构、覆盖缺口/冗余/跨章边界，形成书面结论（缺口清单 + 落点方案，不新增节优先）报用户确认后，再进入下面的节级循环。禁止跳过章级设计直接逐节改。基准文档：《docs/00-模板/全书框架设计学习整理_v1.md》（五部章模板 + 第 7 节检查清单）。

**节组级前置（每个 X.Y 节组复检开始前必做，2026-08-25 用户确立）**：进入 X.Y.1 之前，先对该节组整体做设计调研——大纲对该节组的设计意图、当前小节划分是否充分必要、组内顺序与衔接是否顺畅、与前后节组（X.(Y-1) / X.(Y+1)）的边界与重叠，形成书面结论报用户确认后，再逐小节复检。禁止跳过节组设计直接逐小节改（2026-08-25 第 7 章 7.4 跳步教训：7.1~7.3 均未做节组级调研，用户当场拦下）。

**节级循环（每节走一遍）**：

1. **通读**：整节读完再动手，不顺手改。
2. **事实核对**：所有内核函数名、文件路径、行号、CONFIG 项、结构体字段，必须对 v6.6 源码逐条验证，禁止凭记忆写"内核是这样的"。**示例代码引用具体驱动时，必须对该驱动源码逐行核实**——复检覆盖框架层不等于示例正确（2026-08-27 pca953x 教训：框架核对全过，但示例代码是虚构的）。
3. **衔接与磕绊检查**（用户明确要求的红线）：
   - 相邻小节是否重复讲同一结论/同一操作（措辞近似的重复最刺眼）；
   - 指针类表述是否指对位置（"下一节/上文/前文"是否真在那里）；
   - 「下一步」预告是否与下一节实际标题、内容一致。
4. **补图检查**（2026-08-12 用户要求新增）：评估该节是否需要图辅助理解，按需四选一：
   - **内联 SVG 原理图**（2026-08-13 用户确认）：凡"接线/电路连接"关系（点对点物理连接），手绘内联 SVG 原理图——芯片方框+引脚名与物理引脚号、锯齿电阻/开关/二极管符号、GND 三横线符号、上拉电阻汇电源轨、跨线半圆拱。一律 `stroke="currentColor"` 适配深浅色主题，`viewBox` + `style="max-width:800px;width:100%"`。**禁止用 ASCII 连线图或纯表格表达接线**（用户反馈两者都看不懂）。**SVG 块内严禁空行与注释分组**——Python-Markdown 的 HTML 块遇空行即截断，后续元素成孤儿标签不渲染（2026-08-13 踩坑实录）；SVG 必须压成连续行。
   - **ASCII 图**：层级/流程简单、纯文本能表达清楚的，直接在正文用代码块画（如调用链树、字段布局、时序波形）；
   - **mermaid 图**：有时序/状态/分支结构的，直接补 mermaid（flowchart/sequenceDiagram/stateDiagram-v2）；
   - **生图占位**：需要精细视觉表达（硬件布局、缓存行结构、多版本共存这类空间关系）时，插入 HTML 注释占位块，含完整生图提示词（图名、风格、元素、布局、配色、比例），格式：`<!-- 【待补图】images/<节号>-<名称>.png（优先级：★必要/△可选）\n图名：…\n生图提示词：… -->`。用户人工复检时把提示词交给生图 AI 作图。占位块清单要进批次汇报和章存档。
5. **风格检查**：对照 `plan.md` R1–R22 + 下方第 5 节补充约定。
6. **修复**：用 Edit 精准修改，小修为主；重复内容按第 6 节收敛模式处理。
7. **构建验证**：`"/c/Users/75672/AppData/Local/Python/pythoncore-3.14-64/python.exe" -m mkdocs build 2>&1 | grep -c WARNING` 必须为 **0**。**严禁用托管运行时 `python`**——它没装 mkdocs，命令静默失败导致 grep 计数恒为 0，等于没验证（2026-08-13 踩坑实录：当会话多次误报"0 WARNING"）。pypinyin 缺失警告属环境问题非内容问题，出现时在汇报中说明。
8. **汇报**：逐节给"改/不改 + 原因"判定，列修改点表格；不夸大、不掩盖。

## 4. v6.6 源码核对基础设施

- 本地缓存：`help-docs/kernel-src-v6.6/`，文件名为原路径 `/` 换 `_`（如 `drivers_base_dd.c`、`include_linux_of.h`）。
- 缺失时下载：先 `curl https://raw.githubusercontent.com/torvalds/linux/v6.6/<路径>`；**GitHub 429 限流时改走 jsdelivr 镜像** `https://cdn.jsdelivr.net/gh/torvalds/linux@v6.6/<路径>`（2026-08-12 起生效）。限流时停等换源，不硬刷。
- **行号引用但书**：镜像暂时取不到的文件，正文只引路径不标行号，批次汇报中明示待验项，镜像恢复后补验闭环。
- **行号引用必须基于缓存文件实际查证**，写入正文时注明文件与行号。
- **勿凭旧版本记忆写内核事实**：内核实现随版本变化大，记忆中的"经典实现"可能是旧版或已被移除（2026-09-01 例：IRQS_INPROGRESS 在 v6.6 已不存在，"正在处理中"状态在 `irq_data` 的 `IRQD_IRQ_INPROGRESS` 位）。

## 5. 风格约定（plan.md 之外的补充，全书已确认）

- 提示框三级：`> 💡`（技巧）、`> ⚠️`（注意）、`> 🔴`（危险/硬性前提），81 个文件在用，不得新增级别；emoji 后第一个词**不加粗**；框内不加"提示/陷阱/危险"标签词。
- **概念卡片**：`> 概念名：一句话解释`（不带 emoji，与 💡/⚠️/🔴 三级提示框区分），用于"首次出现的关键概念需要明确定义"处，就地给定义、不打断正文流。扩展篇标配（§9 第 3 条），2026-09-02 用户拍板主线章节同样适用。
- 导读收尾句式：`本节覆盖：…`（禁"学完本节，你能…"）。
- **本节总结=散文收口，表格降级为速查表**（2026-09-02 用户确立）：「本节总结」用 2~4 句带立场的散文收口（定性判断 + 什么时候用/不用 + 最值得记住的一点），内容按节定制、防模板化，禁"本节讲了 A、B、C"复读式；原要点表格保留但改名为「速查表」，定位是日后回来查的工具，不冒充叙事收尾。参照系：LDD3 的 Quick Reference 是独立速查栏目、LKD/CSAPP/宋宝华书均为散文收口，成名著作无一用表格做总结。
- 代码块不带 `$` 提示符。
- 禁生活化比喻（第 11 章 11.2.4 的户籍比喻已因此拆除），技术直述。
- 图片：正文引用 `images/<文件名>.png`，alt 文本描述图内容；改图先改 `tools/` 下生成脚本，重新生成后用 ReadMediaFile 回读验证。
- 换行符统一 `<BR>` 大写。
- **反"悬空"检查**（2026-08-19 用户确立，AI 味的结构性病灶）：每个新小节/新机制出现时，读者此刻脑子里不该冒出"为什么突然讲这个"。判定方法：通读时在每个标题处问"它承接上一段的哪个需求"。两类典型悬空——①**机制先行、动机缺失**：直接给"怎么做/是什么"，没给"为什么需要它"（11.2.2 双向配对的病因：没先讲设备与驱动来自两个互不知情的世界）；②**抽象先行、实例迟到**：抽象角色讲完了具体麻雀还没出现（11.2.2 的全貌在下一节 11.3.1）。修法不是加内容，是补因果段或前置最小实例（几行即可），并在导读后给"全貌地图"预告后文顺序的理由。单个知识点都正确 ≠ 通读顺滑，复检必须含整节通读一遍。
- **内容为主题服务**（2026-08-25 用户重申）：每个知识点都要能回答"它为本节主问题/章主线贡献什么"，讲不清贡献的删掉或挪走；每个设计性要求（为什么读这段、为什么做这步）都在文内给出理据，不让读者自行脑补。**大纲的章级结构（如第二部四段式）是章级叙事意图，不是节级标题模板**——禁止把"问题场景/机制深入"等作为字面小标题逐节重复 stamp（2026-08-25 第 8 章 8.1 批误用教训，用户判定"枯燥且过于模板化"）；节内行文按第 9 节「机制两问」自然展开。

## 6. 重复内容的收敛模式（第 11 章确立）

发现跨节重复时，不两边都留全量：

1. **细节更深的一方保留全量**；
2. 另一方压缩为一两句 + 显式指针（"这套方法在 X.Y.Z 已经讲过，这里不再重复"）；
3. 若重复本身是"不同视角的同一结论"，改写成显式呼应（"X.Y.Z 从工程角度给出过同样结论，本节把它落到机制层面"），让重复读起来是有意的分层，而非失控的冗余。

## 7. index.md 状态列口径

「是否人工复检」列取值：`是`（人工检完）/ `AI复检完成`（AI 检完待人工）/ `待人工复检` / `否`（未检）。AI 复检闭环一章后：改为 `AI复检完成` + 更新日期（当前日期从会话时间戳取），**章级存档写入 `docs/00-模板/AI复检归档.md`**，不写进本文件。

## 8. 已确立的设计决策（勿推翻）

- **驱动教学不塞进 B 扩展**：第 11 章（接合机制）/ B 扩展（总线传输 API）/ D 扩展（子系统写法，待建，第一波最高优先级）/ 第 22 章（选型决策）四层正交。依据见 `docs/00-模板/扩展篇设计大纲_v1.md`。
- **驱动三支柱复检顺序**：第 10 章（中断与时间）→ 第 11 章（设备模型，已完成）→ 第 13 章（并发与同步）。10+11 充分于"读懂驱动"，加 13 才充分于"动手写"的机制层。
- **C 扩展不删不合并**：15 篇成文是边缘 AI/异构多核/实时化/内核演进唯一实际内容，与第五部骨架不冲突；待第 32–37 章撰写时逐专题决策。
- 全书实锚点硬件：**RK3568**（第 11 章 uart2/GIC_SPI 118→hwirq 150 等均按 rk3568.dtsi 实际核对）。

## 9. 扩展篇写作标准（2026-08-17 确立，全书扩展篇适用）

1. **去三件套**：不写「常见陷阱」「动手练习」「下一步」节；其中有保留价值的内容揉进正文（如 lane 极性反接并入排障表展开）。保留：方案对比表、本节总结自查表、配套资源。
2. **厚章节**：少标题、多字数，每节是完整论证链而非要点罗列（标杆：B-F.16.1 约 300 行/29 KB，8 个章节）。
3. **零基础展开**：所有术语首次使用处就地解释；概念用卡片形式补充——`> 概念名：解释`（不带 emoji，与 💡/⚠️ 框区分）。标杆：F.16.1 共 9 张卡片。
4. **机制两问行文 + 一问选材**（用户认可版）：行文只要求「它解决什么问题 → 它怎么工作」自然展开，无固定段式；「失败症状」不做行文模板，只作写作时的详略判断——症状鲜明且常见的机制讲细并收进该篇排障部分，症状模糊的概述即可。防模板化。
5. **工具输出逐段解读**：lspci/dmesg 等给完整真实输出再逐段拆开讲，不给命令清单。
6. **自查表即验收标准**：每行是"读完应能独立完成的动作"，正文对着它检查覆盖度。
7. **PCIe 系列教学基线切分**：10.1~10.3 以 Gen1~Gen3 为基线只讲机制恒定部分；Gen4/5/6 演进（PAM4/FLIT/FEC/AER/降速定位）集中在 10.4；物理层机制引用 F.16.1 不重复展开。篇幅：10.1 约 400~450 行，10.2 约 500~550，10.3 约 550~650，10.4 约 450~500，10.5 约 350~400（选读定位），10.6 约 600~700（实战代码级）。
8. **写作自检循环（每篇写完必走，2026-08-25 用户确立）**：写作侧长期只有标准没有检查，是 B 扩展 44 篇返工的流程根因。每篇成文后按下序自检，全部通过才进汇报：
   1. **标准对照**：逐条对照本节 1~7 条验收（三件套是否残留、是否要点罗列化、术语是否就地解释、行文是否机制两问、工具输出是否逐段解读、篇幅是否达基线）；
   2. **事实核对**：函数名/配置项/命令输出/参数值逐条对源码或官方文档验证，禁止凭记忆（与复检同标准）；
   3. **风格检查**：`plan.md` R1–R22 + §5 补充约定；
   4. **补图检查**：是否需要图，四选一（内联 SVG / ASCII / mermaid / 生图占位）；
   5. **构建验证**：mkdocs build 0 警告（用用户 Python，命令同 §3 第 7 步）；
   6. **自查表验收**：对着本篇「本节总结自查表」逐行问"读者能否独立完成这个动作"，不能就回正文补；
   7. **汇报**：逐项声明检查结果，不达标项明示并列入待办，不掩盖。

## 10. 工作纪律

- **复检修改批量上限**（2026-08-25 用户两次强调确立）：复检修复**以一节为单位推进**——通读、修复、构建验证、汇报一节一闭环，最多不超过 1~2 节再停手等确认；**禁止一次性跨多节甚至跨章连改**（用户原话："不要再一次改很多节了，我怕你丢失注意力，我们慢慢来"）。节内小修不受此限；跨节的统一性修改（如所属行批量统一）也须先报方案、用户确认后一次只做一节。
- **单会话写作批量上限**（2026-08-18 用户反馈确立）：一次连续写作不超过 2~3 篇厚章节，达到即停，汇报后等用户确认再继续。B 扩展赶工期的教训——单会话大批量推进导致注意力衰减，后期篇目行数掉到 100~150 行、内容浮于表面，44 篇列入返工清单。宁可慢，不可薄。
- **外部供稿逐条核实**（2026-08-18 B-E.15.5 合并教训）：外部 AI 或其他来源的供稿/评审，吸收前必须逐条核实——事实性内容（数字、型号、参数、函数名）对源码或官方文档验证，结构性建议先确认原文确实缺失再采纳（2026-08-31 第三份评审 6 条建议仅 2 条半成立）。只取内容不取表述时，注意对方文风可能违反 plan.md。
- 一切生成物放工作区内；不改 `help-docs/` 下的原始 docx。
- 先读后改；不确定的事实用源码验证，不猜测。
- 用户语言为中文，汇报用中文；结尾给单一明确的下一步建议。
