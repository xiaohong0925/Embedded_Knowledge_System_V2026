# B-F.16.5 USB4 与 Thunderbolt

> 所属章节：第五部 B. 总线协议 > F. 前沿高速串行
>
> 难度：[M] | 预计阅读时间：50 分钟

## 本节导读

USB4 是 USB 家族的一次架构革命：它不再是"一种更快的 USB"，而是一台**协议隧道交换机**——同一根 Type-C 线缆里，PCIe、DisplayPort、USB 3.2 三种协议的流量被打包成隧道包分时复用。一个扩展坞插上去，显示器、网卡、NVMe 硬盘同时工作，靠的就是这套隧道机制。

理解它的两条主线：与前代 USB 的关系、与 Thunderbolt 的血缘。USB4 的技术底子是 Intel 2019 年捐给 USB-IF 的 Thunderbolt 3；Thunderbolt 4/5 则是在 USB4 基础上加强制认证要求的"满血版"。对嵌入式工程师来说，USB4 的意义在于它正在下放——新 SoC 开始集成 USB4 控制器，Type-C 一线通（数据+显示+供电）正在成为高端嵌入式设备的标配。

> 本节覆盖：隧道化架构与三类隧道路径、时间同步（TMU）为什么必须存在、速率档位与 PAM3 编码、USB4 与 Thunderbolt 的版本血缘对照、DP Alt Mode 与 DP 隧道的两条视频路径、线缆的 E-Marker 与主动/被动之分、Linux 的 thunderbolt 驱动与安全级别、嵌入式场景的选型清单与排障。按"架构 → 机制 → 版本 → 易混概念 → 工程落地"展开——先把"隧道交换机"这个心智模型立住，后面的版本表和排障表才有挂的地方。

先修：B-C.7 USB 子系统（枚举与描述符）、B-D.10.1 PCIe（隧道载荷之一）、B-F.16.1 SerDes 通识（PAM3/retimer 术语出处）。

## 隧道化架构：一根线里的三条高速公路

传统 USB 是单一协议总线：线上跑的永远是 USB 包。USB4 改换了思路——它自己定义一套链路层，把**其他协议的数据封装成隧道包**在线上传输，到对端再还原：

```text
                    ┌─────────────────────────────┐
   PCIe 事务 ──────►│ PCIe 隧道适配器              │
                    │      ↓ 封装                 │
   DP 视频流 ──────►│ DP 隧道适配器  ──► USB4 路由器 ──► Type-C 链路
                    │      ↓ 封装                 │      （20/40/80G）
   USB 3.2 数据 ───►│ USB3 隧道适配器              │
                    └─────────────────────────────┘

   对端还原：USB4 路由器按包类型分流 → 各协议适配器解封装 → 原生协议出口
```

> 隧道（Tunneling）：把 A 协议的完整数据包当作"货物"装进 B 协议的包格式里传输，到目的地拆封还原。对 PCIe/DP 来说，它们根本不知道自己经过了 USB4——就像集装箱里的货物不知道自己坐的是船还是火车。这是 USB4 与"USB 兼容模式"的本质区别：不是转接，是原样搬运。

三类隧道的分工：

| 隧道 | 载荷 | 典型应用 |
|------|------|---------|
| PCIe 隧道 | 原生 PCIe 事务（TLP） | 扩展坞里的网卡/NVMe/万兆网口、外置显卡 |
| DP 隧道 | DisplayPort 视频流 | 一根线带 4K/8K 显示器 |
| USB3 隧道 | USB 3.2 数据 | 兼容传统 USB 设备 |

> 💡
> PCIe 隧道的推论非常重要：扩展坞里的设备对操作系统就是**普通 PCIe 设备**。插上 USB4 扩展坞，Linux 的 `lspci` 会多出一串设备，驱动照常加载——不需要任何"USB4 设备驱动"。理解这一点，USB4 的神秘感就消失了。

架构里的中心角色是**路由器（Router）**：每个 USB4 设备/主机内部都有一个路由器，负责隧道包的复用、分流与转发（多设备级联时中间设备的路由器做转发）。连接管理器（Connection Manager，主机侧固件/驱动实现）负责发现拓扑、建立/拆除隧道路径——它是 USB4 世界里类似"PCIe 枚举"的角色，但实现在主机软件里而非标准固件流程。

<!-- 【待补图】USB4 隧道复用架构与扩展坞实例图（★必要，建议生图）
生图提示词：技术架构图，白底工程蓝图风格，中文标注，横版 16:7。左半部为抽象架构：三个输入方框自上而下"PCIe 事务（TLP）""DisplayPort 视频流""USB 3.2 数据"，各经一个"隧道适配器"小框（标注"封装"）汇入中央"USB4 路由器"大方框，路由器引出一根粗线标注"Type-C 链路 40G 时分复用（TMU 统一时间基准）"。右半部为对应实物场景：笔记本电脑图标经一根 Type-C 线连接"扩展坞"方框，扩展坞内画同样的路由器结构，分出三路：显示器图标（标注 DP 隧道还原）、NVMe SSD 图标（标注 PCIe 隧道还原，旁注"lspci 直接可见"）、U 盘图标（标注 USB3 隧道还原）。两部分之间用虚线箭头对应。扁平矢量风格，蓝色系，细线条，中文标注，无装饰元素。-->

### 一条 40G 链路怎么装下所有东西

扩展坞场景的带宽账（40G Gen 3×2 链路）：

```text
4K60 显示器（DP 隧道，HBR2 压缩前）     ≈ 12~16 Gbps
坞内 NVMe SSD（PCIe 隧道，x1 Gen3 档）  ≈ 8 Gbps
坞内千兆网口 + USB3 U 盘                ≈ 2 Gbps
协议与时间同步开销                      ≈ 10%~15%
─────────────────────────────────────────────
合计                                   ≈ 22~26 Gbps，40G 链路余量充足
```

但如果显示器换成 8K 或双 4K，DP 一路就吃掉 30G+，链路立刻紧张——这就是为什么 Thunderbolt 4 认证强制"双 4K"而 USB4 把它列为可选：**标称速率相同，能兑现的场景不同**。评估坞站方案时按自己负载清单算一遍这笔账，别只看 40G 的标称值。

## 时间同步：三条隧道共享一条链路的代价

三类隧道共享一条物理链路，问题随之出现：DP 视频流是**等时流量**（isochronous）——每帧像素必须在确定的时间窗口送达，晚到就是花屏；而 PCIe/USB3 是异步流量，能容忍排队。

> 等时流量（Isochronous Traffic）：对"送达时刻"有硬要求的数据流。音视频流是典型：带宽可以协商，但每帧的送达窗口是死的，缓冲只能吸收有限抖动。对比异步流量（文件拷贝）：晚到一拍无所谓，总吞吐够就行。USB 2.0 时代的等时传输（audio）就是同一概念，USB4 把它放到了多协议共享链路的更复杂环境里。

USB4 的解法是**时间管理单元（TMU，Time Management Unit）**：链路两端的路由器通过专用协议把本地时钟对齐到 ns 级，然后在统一时间基准上做时分调度——视频流的隧道包被保证在约定的时刻窗口内发出去。这就是为什么 USB4 规范里有一整章讲时间同步，也是"USB4 链路训练完成后 TMU 未锁定"会导致视频异常而数据传输正常的原因。

## 速率档位与编码

| 版本 | 速率 | 编码 | 对称性 |
|------|------|------|--------|
| USB4 Gen 2×2 | 20 Gbit/s | 64b/66b | 对称 |
| USB4 Gen 3×2 | 40 Gbit/s | 64b/66b | 对称 |
| USB4 v2 Gen 4×2 | 80 Gbit/s | **PAM3**（3 电平） | 对称 |
| USB4 v2 Gen 4 非对称 | 120/40 Gbit/s | PAM3 | 显示优先时单向提速 |

两个值得注意的点。一是 USB4 只用 Type-C 接口，Type-A 物理上不存在 USB4——Type-C 的两组高速差分对（TX1/RX1、TX2/RX2）正好支撑"×2"双通道。二是 v2 上 80G 靠的是**编码升级而非频率翻倍**：PAM3 用 3 个电平，每符号传 log₂3 ≈ 1.58 bit，在相同波特率下比 NRZ 多传 58%——B-F.16.1 讲的"带宽不够先升级调制"在这里是个少见的三电平实例（PCIe/车载走的是 PAM4 四电平）。非对称模式 120/40 是显示场景的特化：视频下行吃带宽，上行只需少量回传。

## USB4 与 Thunderbolt：一张血缘图

| 维度 | USB4 v1/v2 | Thunderbolt 3 | Thunderbolt 4 | Thunderbolt 5 |
|------|-----------|---------------|---------------|---------------|
| 发布 | 2019 / 2022 | 2015 | 2020 | 2023 |
| 最高速率 | 40 / 80 Gbit/s | 40 Gbit/s | 40 Gbit/s | 80 / 120 Gbit/s |
| PCIe 隧道 | 可选 | 强制 | 强制（32 Gbit/s 保底） | 强制（64 Gbit/s 保底） |
| DP 支持 | 可选 | 强制 | 强制双 4K | 强制多 8K |
| 认证 | USB-IF 可选 | Intel 强制认证 | 同左 | 同左 |

关系一句话：**USB4 是 Thunderbolt 3 的开放版，Thunderbolt 4/5 是 USB4 的强制满血认证版**。

认证差异的实际含义值得展开：Thunderbolt 的强制认证意味着贴标的设备（线、坞、主机）都过了 Intel 的互操作测试矩阵——你买到的 TBT4 坞插在任意 TBT4 主机上"必然"工作；USB4 的可选认证意味着兼容性靠厂商自觉，两个都标 USB4 的设备插一起不工作是合规可能事件。消费产品（笔记本配件）要 Thunderbolt 认证保口碑；工业定制（两端都是自己定的）可以走非认证 USB4 省认证费——但要把"互操作自测矩阵"补回来：自己测遍所有目标线缆与对端组合。

USB4 把大部分特性定为"可选"，所以同样是标 USB4 的口，不同设备能力差距可能很大——一个只支持 DP+USB3 隧道（不支持 PCIe 隧道）的 SoC 也可以合法自称 USB4。选型时要看规格书的具体条目（PCIe 隧道支持与否、DP 几路、Gen 几），不能只看"USB4"三个字。这条对嵌入式选型尤其重要：很多集成 USB4 的 SoC 砍掉了 PCIe 隧道，扩展坞玩法直接不存在。

## 线缆：E-Marker 与主动/被动之分

USB4 时代线缆不再是"线"，是带芯片的部件：

> E-Marker（Electronically Marked Cable）：USB Type-C 线缆里内嵌的一枚小芯片，通过 CC 线向两端报告线缆能力（支持的最大速率、电流、是否主动线）。主机按它报告的能力决定链路怎么训练。**没有 E-Marker 或虚标能力的线缆是"协商降速/不稳"的高频根因**——排障表里"只当 USB 2.0 用""40G 只协商到 20G"的第一嫌疑都是它。

| 线缆类型 | 长度上限（40G） | 原理 | 成本 |
|:---|:---|:---|:---|
| 被动线 | ~0.8 m | 纯铜线 | 低 |
| 主动线 | 2 m+ | 内置 redriver/retimer 重新驱动信号 | 高 |

> Retimer / Redriver：高速链路的中继器件。Redriver 只做模拟放大与均衡（增强信号），retimer 带时钟数据恢复（CDR），把数据完整重新定时再生——眼图重新张开。B-F.16.1 的器件谱系有展开；在 USB4 场景它们被做进主动线缆的插头里。

## DP Alt Mode 与 DP 隧道：别混淆

Type-C 上的视频有两条完全不同的路径：

- **DP Alt Mode（替代模式）**：Type-C 的高速差分对**直接改接**到 DP 信号源，物理切换（mux），不经过 USB4 链路。USB 3 时代的方案，USB4 之前所有 Type-C 视频输出都是它；今天不支持 USB4 的普通 Type-C 视频口仍是它。
- **DP 隧道**：DP 流封装进 USB4 隧道包，与 PCIe/USB3 流量共存于同一链路。USB4 时代的方式，优势是与数据流量共存且可多路。

<!-- 【待补图】DP Alt Mode 与 DP 隧道路径对比图（★必要，建议生图）
生图提示词：技术对比示意图，白底工程蓝图风格，中文标注，竖版分左右两栏 16:9。左栏标题"DP Alt Mode（物理改接）"：Type-C 插头引出 4 对高速差分线，经一个标注"mux 开关"的菱形直接改接到"DP 信号源"方框，旁注"USB4 链路不参与，CC 逻辑协商后物理切换"，下方画显示器。右栏标题"DP 隧道（封装复用）"：同样 Type-C 插头，差分线进入"USB4 路由器"方框，路由器内三条色带分别标注"PCIe 隧道/DP 隧道/USB3 隧道"汇入一条粗箭头（标注"20/40/80G 时分复用"），到对端路由器分流后 DP 色带接显示器，PCIe 色带接 NVMe 图标，USB3 色带接 U 盘图标。两栏用不同底色区分（左浅灰、右浅蓝），风格扁平矢量、细线条、无装饰。-->

区分它们的意义在排障：Alt Mode 下视频不通是 mux/配置问题（CC 逻辑没协商对）；隧道模式下视频不通是链路训练、TMU 锁定或带宽分配问题。两者的 dmesg 日志路径完全不同——Alt Mode 的日志在 Type-C/PHY 驱动里，隧道的日志在 thunderbolt 驱动里。

> CC 逻辑（Configuration Channel）：Type-C 接口上用于协商"谁供电、谁受电、谁是 Host、进什么 Alt Mode"的专用配置通道（B-C.7.1 提到过的 CC 线）。所有 Type-C 高级功能的入口协商都发生在 CC 上，它不通，后面一切免谈。CC 上的协商由 USB PD 协议承载。

供电是"一线通"的第三条腿：USB PD（Power Delivery）在 CC 线上协商电压/电流档位（5V/9V/15V/20V，PD3.1 扩到 48V/240W）。扩展坞场景的典型功率流是双向的——坞给笔记本充电（上行供电），坞又从自己的电源适配器取电分给下游设备。嵌入式产品做坞站类设计时，PD 协商芯片（如 FUSB302、TPS6598x 系列）是独立于 USB4 控制器的一路器件，两路的固件配合（先 PD 协商供电与角色，再进 Alt Mode/USB4 枚举）是 bring-up 的正确顺序。

## Linux 支持：thunderbolt 驱动与安全级别

内核的 `thunderbolt` 驱动同时管 USB4 和 Thunderbolt（代码同源）。用户态的可见面：

```bash
# 设备与拓扑
cat /sys/bus/thunderbolt/devices/*/device_name
lsusb -t                        # USB4 控制器以 xhci 形式出现
lspci                           # 隧道过来的 PCIe 设备直接可见

# 连接管理（安全级别）
cat /sys/bus/thunderbolt/devices/domain0/security
```

插上 USB4 扩展坞后的典型 `lspci` 增量（判读示范）：

```text
03:00.0 PCI bridge: Intel Corporation JHL6540 ...   ← 隧道进来的桥片
04:01.0 PCI bridge: ...                              ← 坞内下游端口
05:00.0 Ethernet controller: Intel I210 ...          ← 坞里的网卡，普通 PCIe 设备
```

看到 PCI bridge 链就说明 PCIe 隧道已建立；`05:00.0` 这种设备的出现与普通插卡没有任何区别——`lspci -vv` 里它的 LnkCap/LnkSta 显示的是坞内链路的速率，不是 USB4 链路速率，读数时别混淆两层链路。

> 安全级别（Security Level）：Thunderbolt/USB4 的 PCIe 隧道意味着外设可以直接 DMA 访问内存——插上一个恶意扩展坞等于把内存交出去（这类攻击叫 DMA 攻击）。为此定义了 SL0~SL3：SL0 不设防；SL1 用户确认；SL2 设备需预授权（设备里有认证密钥）；SL3 仅 USB/DP 隧道，禁 PCIe。桌面发行版用 `bolt` 守护进程管理授权，嵌入式产品通常固化 SL0 或 SL3，按产品威胁模型选——面向最终用户可插拔的设备选 SL3 起步是稳的。

设备树层面，USB4 控制器节点与 xHCI 类似（参考时钟、PHY、retimer 描述），差异化部分由各家 SoC 厂商定义；多数嵌入式场景下原厂 BSP 已配好，工程师的工作是验证与排障而非从零 bring-up。

## 协商与回退：USB4 不成怎么办

Type-C 口上的能力协商是分层的，理解回退链才能读懂"降级"现象：

```text
插入
  │
  ▼ CC 逻辑（PD 协议）：供电角色、数据角色、线缆能力（读 E-Marker）
  │
  ▼ 尝试 USB4 链路训练
  │   ├─ 成功 → USB4 模式（三类隧道可用，Alt Mode 也可叠加）
  │   └─ 失败 ↓ 回退
  ▼ USB 3.2 原生模式 + DP Alt Mode（各自独立协商）
  │
  ▼ 再失败 → USB 2.0 保底（Type-C 的 2.0 差分对永远可用）
```

排障表里"设备只当 USB 2.0 用"就是回退链一路跌到底的表现——USB4 训练和 USB 3.2 训练都失败了，最常见原因按序：线缆无 E-Marker 或虚标 → CC 协商阶段就有错（PD 分析仪看）→ 高速通道信号完整性（retimer/走线）→ 控制器固件。**回退链的存在意味着"还能用"不代表"没问题"**：设备功能正常但速度慢一倍，用户可能几周都不发现，验收时要主动核对链路协商到的档位（`lspci -vv` 的 LnkSta、内核日志里的 link training 行）。

## 嵌入式视角：什么时候会碰到 USB4

| 场景 | USB4 的角色 |
|------|------------|
| 高端单板计算机/模块化电脑 | 一线通扩展：底座里放 NVMe、网卡、显示输出 |
| 数通/仪器前面板 | 调试口同时承担数据 + 显示 + 供电 |
| 机器人/AGV 主控 | 传感器坞站快速插拔（PCIe 隧道接采集卡） |
| 测试测量设备 | 高速数据回传主机（40G 链路跑 PCIe 隧道，等效外插卡） |

一个展开的案例帮落地——仪器前面板"一线通"：调试笔记本一根线接仪器，仪器侧 USB4 控制器同时跑三条隧道：PCIe 隧道把采集卡数据回传主机（等效仪器变成主机的 PCIe 外设，驱动与台式机插卡完全复用）、DP 隧道把仪器屏显镜像到笔记本外接显示器、PD 让仪器给笔记本供电。研发团队得到的收益是**驱动栈零新增**——所有设备都是原生 PCIe/USB/DP 形态；代价是 SoC 必须支持 PCIe 隧道、硬件要过信号完整性（Type-C 口的板内走线与 retimer），以及固件要把 PD/角色协商/隧道建立的时序状态机做稳。

选型清单（评审一个 USB4 方案时的必查项）：

1. **隧道支持**：SoC 控制器支持哪些隧道？很多只支持 DP+USB3，不支持 PCIe 隧道——扩展坞玩法的前提。
2. **线缆**：被动线 0.8 m 上限（40G），更长要主动线；E-Marker 信息要实测读取验证（`tbtadm`/`boltctl` 或 PD 分析仪）。
3. **认证状态**：影响与第三方设备的兼容性。工业定制场景可接受非认证方案，消费场景不行。
4. **安全级别策略**：产品威胁模型决定 SL 档位，量产固件里固化。
5. **retimer 布局**：板内长走线到 Type-C 口可能要 retimer，原理图评审项。
6. **回退链验收**：协商档位要主动核对，不能只看"功能正常"。

## 排障速查

| 症状 | 第一怀疑 | 验证路径 |
|------|---------|---------|
| 设备只当 USB 2.0 用 | CC 协商失败 / 无 E-Marker 或虚标线 | 换认证线缆交叉；PD 分析仪看 CC 协商 |
| 40G 链路只协商到 20G | 线缆不达标（被动线超长）；retimer 固件 | 换短线复现；查链路训练日志 |
| 扩展坞里 PCIe 设备不出现 | 安全级别拦截 | `cat domain0/security`，查 bolt 状态 |
| 视频无输出但数据正常 | DP Alt Mode 与 DP 隧道路径选错；mux 配置 | dmesg 区分 Type-C/PHY 日志 vs thunderbolt 日志 |
| 视频花屏但传输正常 | TMU 未锁定，等时流量窗口漂移 | 查 TMU 锁定状态；换低分辨率复现 |
| 插拔后链路不恢复 | 连接管理器固件状态残留 | 换端口复现定位；升级控制器固件 |
| 坞内设备时有时无 | 供电不足（坞的 PD 功率预算被超分） | 查坞的供电规格与负载清单 |
| 隧道建立但速率低于预期 | 链路训练落在低档；两端 Gen 能力取交集 | 内核日志查 link training 结果；核对两端 Gen 档位 |

## 本节总结

USB4 把 USB 从"一种总线"改造成了"一台协议隧道交换机"：PCIe/DP/USB3 三类流量封装成隧道包，在一条 Type-C 链路上由路由器复用分流，TMU 时间同步保证视频等时流量的送达窗口。它的工程推论很反直觉但极实用：隧道过来的设备就是原生设备（lspci 直接可见，无需新驱动），所以调试 USB4 问题实际上是调试 PCIe/DP/Type-C 三个老领域的问题，只是多了一层链路训练与安全级别。版本血缘上记住"USB4 是 TBT3 开放版、TBT4/5 是强制满血版"，选型盯规格书具体条目而非 USB4 三个字。线缆在 40G 时代成了带芯片的部件（E-Marker/主动线），排在排障怀疑列表的前列。

| 关键结论 | 一句话记忆 |
|:---|:---|
| 本质 | 隧道交换机：三种协议打包复用一条 Type-C 链路 |
| 最大推论 | 隧道设备对 OS 是原生设备，lspci 直接可见 |
| TMU | 等时视频流的送达窗口靠 ns 级时间同步保证 |
| 版本血缘 | USB4=TBT3 开放版；TBT4/5=强制满血认证版 |
| 两条视频路 | Alt Mode 是物理改接，隧道是封装复用，排障入口不同 |
| 线缆 | E-Marker+主动/被动之分是降速类故障首查 |
| 回退链 | USB4 失败会静默降级到 3.2/2.0，验收要核对协商档位 |
| 一线通 | 数据+显示+供电三条腿：隧道/DP 隧道或 Alt Mode/PD 在 CC 上先协商 |

## 本节自查

读完本节，你应能独立做到：

- 画出三类隧道经路由器复用一条链路的结构，解释"不是转接是搬运"
- 说明 TMU 存在的原因，以及"TMU 未锁定"为什么只影响视频不影响数据
- 说清 USB4 v1/v2 与 Thunderbolt 3/4/5 的对应关系与强制项差异
- 解释 USB4 v2 靠 PAM3 而非频率翻倍到 80G，算出每符号比特数
- 区分 DP Alt Mode 与 DP 隧道，并说明排障时的不同切入点与日志路径
- 用 sysfs/lspci/bolt 验证隧道设备与安全级别，判读 lspci 里两层链路速率
- 评审一个嵌入式 USB4 方案时列出五项必查项
- 解释 SL0~SL3 安全级别的威胁模型（DMA 攻击），为给定产品选档
- 画出插入后的协商与回退链，解释"还能用不代表没问题"的验收原则
- 算一条 40G 链路的带宽账，判断给定负载组合能否共存
- 说明 Thunderbolt 强制认证与 USB4 可选认证在兼容性上的实际差异
- 说出 PD 协商与 USB4 枚举在 bring-up 中的先后顺序

## 配套资源

- USB4 Specification v1.0/v2.0（usb.org，公开下载）
- Intel Thunderbolt 技术简报（thunderbolttechnology.net）
- 内核文档：`Documentation/admin-guide/thunderbolt.rst`
- bolt 项目（用户态授权管理）：gitlab.freedesktop.org/bolt/bolt
- USB Type-C Specification Release 2.x 与 USB PD 规范（CC 协商细节）
- 本书关联：B-C.7（USB 子系统）、B-D.10.1（PCIe，隧道载荷）、B-F.16.1（PAM3/retimer/redriver 通识）
- USB PD 3.1 规范（EPR 240W 档位）；VESA DisplayPort Alt Mode on Type-C 规范
