# B-B.4.4 SPI 调试与选型

> 所属章节：第五部 B. 总线协议 > B-B.4 SPI 总线
>
> 难度：[I] Intermediate | 预计阅读时间：30 分钟

## <span class="blue"> 本节导读

SPI "用起来简单，调起来头疼"：没有 ACK 机制，任何配置错误都表现为静默的数据错乱。但 SPI 故障的分布高度集中——模式、速率、CS、信号完整性四类占了绝大多数，排查路径因此可以收敛成固定流程。本篇前半是排障方法论，后半解决另一个工程问题：新方案设计时，这颗外设该选 SPI 还是 I2C。

本节覆盖：SPI 排查三原则与决策树、五类高频故障对照、spidev_test 回环与内核侧观察、示波器/逻辑分析仪使用要点、SPI vs I2C 选型对比、典型设备的接口选择、QSPI/OSPI 的选型决策。

---

## <span class="blue"> 排查三原则与决策树

SPI 没有 ACK 告诉你"收错了"，排查必须从最便宜的假设开始逐项排除：

```
① 核模式（成本 1 分钟）
   CPOL/CPHA/位序与从设备手册逐项对照——最高频故障
   ├─ 不一致 → 改 spi_mode 收工
   └─ 一致 ↓
② 降速验证（成本 5 分钟）
   SCLK 降到 1 MHz 甚至 100 kHz
   ├─ 降速后正常 → 信号完整性问题（走线/端接/飞线），回 B-B.4.1
   └─ 依旧异常 ↓
③ 抓波形对照（上仪器）
   按 B-B.4.2 的分段法：命令/地址/dummy/数据逐段对手册
   CS 是否全程稳定？SCLK 频率实测是否等于配置值？
```

先用软件手段（①②）排除八成的逻辑类故障，再上仪器（③）处理剩下两成的信号类故障。**顺序反了是 SPI 调试最常见的浪费时间方式。**

---

## <span class="blue"> 五类高频故障对照

| 故障 | 症状 | 根因 | 定位 | 处理 |
|------|------|------|------|------|
| 模式不匹配 | 数据错位、全 0x00/0xFF、位镜像 | CPOL/CPHA/位序与手册不一致 | ①核配置；波形看 SCLK 空闲电平 | 更正 mode/位序 |
| 速率过高 | 偶发错误、温度相关、重试可恢复 | 超从设备上限或走线 SI 极限 | ②降速二分 | 降速/缩线/串 22~47 Ω |
| CS 异常 | 多收/少收字节、帧边界乱 | CS 毛刺、GPIO 模拟时序不严、事务中途拉高 | 示波器单次触发抓 CS | 硬件 CS、查驱动逻辑 |
| 信号完整性 | 随机错、无响应、过冲振铃 | 走线长、无地回路、无端接 | 示波器看过冲/边沿 | 缩线/端接/降速 |
| 上电时序 | 偶发启动失败、首笔超时 | 从设备未就绪主设备就发命令 | 电源与 CS 同屏观察 | probe 加延时、查复位时序 |

> 💡 走线超 30 cm 且时钟超 10 MHz 时反射不可忽略：振铃让从设备在错误边沿采样，表现为"随机"错误。修复三选一：缩到 10 cm 内、源端串 22~47 Ω、降到 1 MHz 以下。

---

## <span class="blue"> 工具链

### 回环与内核侧

```bash
# 回环测试：MOSI 短接 MISO，验证控制器+pinctrl（问题二分到控制器侧/从设备侧）
spidev_test -D /dev/spidev1.0 -v

# 内核侧观察
dmesg | grep -i spi            # 控制器注册、spi-nor 识别日志
ls /sys/bus/spi/devices/       # 设备实例化确认
cat /sys/bus/spi/devices/spi1.0/modalias    # 匹配的 compatible
```

### 示波器四要点

1. **SCLK 实测频率与占空比**：分频取整可能让实际频率偏离配置值；占空比严重偏离 50% 说明驱动或负载问题
2. **数据边沿位置**：CS 下降沿触发，对照手册确认采样边沿——判断模式匹配的最直观方法
3. **CS 质量**：单次触发抓 CS 全程，几十纳秒的毛刺也足以让从设备误判帧边界
4. **幅度与过冲**：高电平应 ≥90% VCC；过冲超 10% 说明阻抗不匹配，需端接

### 逻辑分析仪

```
接线：SCLK / MOSI / MISO / CS 四通道 + 共地
采样率：≥ SCLK × 4（8 MHz 总线用 32 MHz 以上）
触发：CS 下降沿，预触发 25~50%
解码器：SPI，Enable=CS Active Low，Bit Order=MSB First
```

解码结果按 B-B.4.2 分段法对照手册：命令/地址/dummy/数据逐段核对。CPOL/CPHA 不确定时，解码器里切换组合直到数据合理。

---

## <span class="blue"> SPI vs I2C 选型对比

| 维度 | SPI | I2C |
|------|-----|-----|
| 速率 | 1~50 MHz（QSPI 更高） | 100k~1 MHz |
| 引脚 | 4 线起，每从机 +1 CS | 恒定 2 线，挂百余设备 |
| 双工 | 全双工 | 半双工 |
| 寻址 | 无地址，靠 CS | 7/10 位地址帧 |
| 应答/流控 | 无 ACK、无流控 | ACK 握手 + 时钟延展 |
| 多主 | 不支持 | 原生仲裁 |
| 标准规范 | 无统一标准，时序因厂而异 | NXP UM10204 严格规范 |
| 故障可见性 | 静默错乱（无 ACK） | NACK 可直接观测 |
| 功耗 | 推挽，静态功耗低 | 上拉电阻持续耗电 |

决策一句话：**要速度、要全双工、点对点——SPI；要省引脚、多设备、多主、低速控制——I2C。**

## <span class="blue"> 典型设备的接口选择

| 设备类型 | 主流接口 | 速率 | 选择理由 |
|----------|----------|------|----------|
| NOR Flash | SPI/QSPI | 20~80 MHz | 吞吐决定烧录与启动时间 |
| OLED/LCD 小屏 | SPI | 10~40 MHz | 帧数据量大；大屏视频用 RGB/MIPI（B-B 板块） |
| ADC/DAC | SPI 为主 | 1~20 MHz | 采样率与时钟直接相关 |
| RF 收发器 | SPI 为主 | 1~10 MHz | 寄存器配置 + FIFO 数据 |
| 温湿度传感器 | I2C 为主 | 100~400 kHz | 数据量小，省引脚 |
| 加速度计/陀螺仪 | 两者皆有 | 高采样选 SPI | >1 kHz 采样率 I2C 吃力 |
| 触摸屏 | I2C 为主 | 100~400 kHz | 坐标数据量小 |
| EEPROM | 小容量 I2C / 大容量 SPI | — | 分界线约 64 KB |

### QSPI/OSPI 的选型决策

机制已在 B-B.4.2 讲过，这里只给选型视角：

- **XIP 就地执行**（代码直接跑在 Flash 上）：QSPI 是事实标准
- **大容量固件/资源存储**（64 MB+）：OSPI 读速接近 SD 卡
- **成本敏感小系统**：标准 SPI Flash 兼容性最好

> ⚠️ QSPI/OSPI 的带宽优势几乎只在**读路径**：写入（页编程）多数仍走单线模式，且受 Flash 内部编程时间限制（ms 级）。频繁写入的场景（数据记录器），多线模式帮不上忙——选型时看的是写速，别被读速宣传误导。

---

## <span class="blue"> 方案对比（Trade-off）

| 维度 | 评价 |
|------|------|
| 硬件 CS | 时序精确无毛刺；代价是占用控制器片选资源 |
| GPIO 模拟 CS | 片选数量不受限；代价是时序受调度影响、毛刺风险 |
| 示波器 | 信号质量金标准；代价是贵、长时捕获弱 |
| 逻辑分析仪 | 长时捕获+协议解码便宜；代价是看不到模拟特性 |
| 高速 QSPI | 读吞吐 ×4；代价是引脚、布线、写路径无收益 |

---

## <span class="blue"> 常见陷阱

> ⚠️ 跳过 ①② 直接抓波形。仪器架起半小时，问题是模式写错——软件核对一分钟的事。

> ⚠️ 分频取整后不看实测频率。配置 50 MHz 实测可能 48 或 62.5 MHz，超从设备上限时偶发错误。示波器量实际 SCLK。

> ⚠️ GPIO 模拟 CS 在多线程环境被抢占。CS 拉低到数据传输之间被调度出去，从设备看到的是超长事务。关键段加锁或用硬件 CS。

> ⚠️ 多从设备共用总线却忘了模式不同。设备 A 用 Mode 0、设备 B 用 Mode 3，切换设备时忘切模式。每次选中设备前重新 `spi_setup()`。

> ⚠️ 被 QSPI 读速宣传误导选型。写密集场景 QSPI 无收益，页编程时间是物理限制。

---

## <span class="blue"> 动手练习

1. **三原则实操**：人为制造一个模式错误（spidev 设错 mode），观察症状；再走一遍 ①②③ 流程定位，记录每步耗时。
2. **实测频率**：配置两个不同 SCLK 目标值，示波器或逻辑分析仪测实际频率，计算分频取整偏差。
3. **回环二分**：MOSI 短接 MISO 跑 `spidev_test -v`，验证控制器侧；断开短接从设备侧读 ID（如 0x9F），完成一次完整二分定位。
4. **无硬件后备**：拿一份实际项目的选型需求（如"1 kHz 采样的六轴 IMU + 16 MB 固件存储 + 温湿度计"），用本篇两张表给出接口分配方案并写出理由。

---

## <span class="blue"> 本节总结

| 自查项 | 确认标准 |
|--------|----------|
| 三原则 | 核模式 → 降速 → 抓波形，成本递增顺序 |
| 五类故障 | 模式/速率/CS/SI/上电时序的症状与处理 |
| 工具 | 回环二分、dmesg/sysfs、示波器四要点、分析仪设置 |
| 选型 | SPI vs I2C 决策一句话；典型设备接口表 |
| QSPI 决策 | 读路径收益、写路径无收益、XIP 场景 |

---

## <span class="blue"> 配套资源

- **仪器**：DSLogic / Saleae Logic（SPI 解码）、示波器
- **工具**：`spidev_test`（内核 `tools/spi/spidev_test.c`）
- **阅读**：NXP AN3947（SPI PCB 布局）、Eric Bogatin《信号完整性与电源完整性分析》

---

## <span class="blue"> 下一步

调试方法论之后是收官实战：**B-B.4.5 实战：W25Qxx SPI NOR Flash 端到端**——设备树 → spi-nor → MTD 分区 → `flashcp`/`flash_erase` 烧录校验 → JEDEC ID 与页编程的底层验证，把 SPI 板块四篇知识全部落地。之后进入 **B-B.5 UART**。

> 💡 螺旋衔接：本篇决策树与 B-B.3.4 I2C 决策树是同一方法论的两条实例链，对比阅读可固化"先软件后仪器"的排障习惯；回环测试的原理就是 B-B.4.1 的全双工结构；写路径的器件行为约束在 B-B.3.5 与 B-B.4.5 两次实战中都有一席之地。
