# B-B.4.1 SPI 物理层与四种模式

> 所属章节：第五部 B. 总线协议 > B-B.4 SPI 总线
>
> 难度：[B] Beginner | 预计阅读时间：25 分钟

## <span class="blue"> 本节导读

SPI（Serial Peripheral Interface）用更多的信号线换更高的速度和更简单的协议：没有地址帧、没有 ACK 握手、没有上拉电阻，主设备拉低片选就开始全双工收发。代价是协议几乎没有"约束"——时钟极性、采样边沿、位序全靠主从双方事先约定，四种模式（CPOL/CPHA 组合）就是 SPI 调试最高频的故障根源。

本节覆盖：四线接口与全双工结构、四种模式的本质与波形、速率计算与限制因素、多从设备拓扑（独立 CS vs 菊花链）、推挽电气特性与信号完整性。

---

## <span class="blue"> 四线接口与全双工

SPI 由 Motorola 在 1980 年代提出，主从架构：主设备发起并控制一切，从设备被动响应。

| 信号线 | 方向（主视角） | 功能 |
|:---:|:---:|:---|
| SCLK | 输出 | 串行时钟，主设备产生 |
| MOSI | 输出 | Master Out Slave In，主发从收 |
| MISO | 输入 | Master In Slave Out，从发主收 |
| CS/SS | 输出 | 片选，低电平有效，每个从设备一根 |

```
     主设备                              从设备
  ┌──────────┐                        ┌──────────┐
  │          │────── SCLK ───────────→│          │
  │   SPI    │────── MOSI ───────────→│   SPI    │
  │  控制器   │←───── MISO ───────────│  从设备   │
  │          │────── CS（低有效）─────→│          │
  └──────────┘                        └──────────┘
```

MOSI 与 MISO 各自独立，所以 SPI 是**全双工**：每个时钟周期双方各发 1 位，理论效率是半双工 I2C 的两倍。CS 每从设备一根，主设备拉低哪根就和谁通信，其余从设备忽略总线活动。

> 💡 厂商命名有差异：TI 文档中 MOSI 可能叫 SIMO、MISO 叫 SOMI；部分新资料改用 COPI/CIPO 或 SDO/SDI。看原理图按方向对，不认名字。

与 I2C 的关键差异，一句话说清：**I2C 靠地址帧在共享总线上点名，SPI 靠硬件片选线直接选中**——SPI 没有寻址开销，也因此没有地址冲突问题，但每个从设备多吃一根 CS。

---

## <span class="blue"> 四种模式：CPOL 与 CPHA

SPI 协议本身不规定时钟形态，由两个参数约定：

- **CPOL**（时钟极性）：SCLK 空闲电平。0=空闲低，1=空闲高
- **CPHA**（时钟相位）：数据在第几个边沿采样。0=第一个边沿，1=第二个边沿

| 模式 | CPOL | CPHA | 空闲电平 | 采样边沿 | 典型设备 |
|:---:|:---:|:---:|:---:|:---:|:---|
| Mode 0 | 0 | 0 | 低 | 上升沿 | 最常见：W25Q 系列 Flash、多数 ADC/DAC |
| Mode 1 | 0 | 1 | 低 | 下降沿 | 部分传感器（ADXL345） |
| Mode 2 | 1 | 0 | 高 | 下降沿 | 少见 |
| Mode 3 | 1 | 1 | 高 | 上升沿 | SD 卡 SPI 模式、CC1101 射频 |

### Mode 0 时序（CPOL=0, CPHA=0）

```
CS:   ──┐                                    ┌──
        └────────────────────────────────────┘
SCLK: ────┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌───
          └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘
          ↑   ↑   ↑   ↑   ↑   ↑   ↑   ↑
          上升沿采样（数据在下降沿变化）
MOSI: ───╪══╪══╪══╪══╪══╪══╪══╪───
         D7 D6 D5 D4 D3 D2 D1 D0   （MSB 先行）
```

规则：空闲低电平；发送方在下降沿更新数据，接收方在上升沿采样——采样点落在数据稳定区中央，满足建立/保持时间。

### Mode 3 时序（CPOL=1, CPHA=1）

```
CS:   ──┐                                    ┌──
        └────────────────────────────────────┘
SCLK: ─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐
       └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └───
         ↑   ↑   ↑   ↑   ↑   ↑   ↑   ↑
         下降沿采样（数据在上升沿变化）
MOSI: ───╪══╪══╪══╪══╪══╪══╪══╪───
         D7 D6 D5 D4 D3 D2 D1 D0
```

与 Mode 0 对比：空闲电平反相、采样换到第二个边沿。四种模式没有优劣，完全由从设备硬件决定——**主设备必须配置成与从设备一致**。

> ⚠️ 主从模式不匹配是 SPI 最高频故障。Mode 0 主设备配成 Mode 3：采样边沿错了半拍，读回的数据整体移位或全乱，且**不报任何错误**——SPI 没有 ACK 机制告诉你"收错了"。排查"SPI 数据错乱"时先核对模式，再怀疑硬件。

> 💡 手册没写清模式时的反推法：逻辑分析仪抓波形，SCLK 空闲电平定 CPOL，数据在哪条边沿稳定/变化定 CPHA。比逐个模式试错快得多。

---

## <span class="blue"> 速率计算与限制因素

SPI 无仲裁、无应答开销，速率由主设备 SCLK 频率直接决定。以 RK3568 为例：SPI 控制器（`spi@fe620000` 等）的时钟由 CRU 分频产生，手册上限 50 MHz 级别；实际能跑多快由更苛刻的一方决定。

| 限制因素 | 说明 | 典型量级 |
|:---|:---|:---|
| 从设备上限 | 每颗从芯片有自己的 SCLK 上限 | NOR Flash 50~133 MHz；传感器常只有几 MHz |
| 分频粒度 | 实际速率 = 源时钟 / 整数分频 | 目标值常无法精确命中，取不大于目标的最近档 |
| 走线质量 | 信号完整性随速率恶化 | 杜邦线飞线几 MHz 就到头 |
| 软件模拟 SPI | GPIO bit-bang 受 CPU 与延时精度限制 | 通常 ≤1 MHz |

经验法则：**先按从设备手册上限的 1/2 起跑，验证稳定后再逐步提速**；读数偶发出错时第一反应是降速验证，而不是改逻辑。

---

## <span class="blue"> 多从设备拓扑

### 独立 CS（主流）

SCLK/MOSI/MISO 三线并联到所有从设备，每个从设备一根独立 CS（SoC 侧用 GPIO 或控制器原生 CS）。N 个从设备 = N+3 根线。各设备独立访问，模式可以不同（每次通信前切换配置）。RK3568 的 SPI 控制器原生支持 cs0/cs1 两片选，更多从设备用 GPIO 扩展。

### 菊花链（Daisy Chain）

```
主 MOSI → [从1] MISO → [从2] MISO → [从3] MISO → 主 MISO
         SCLK、CS 并联到全部从设备
```

数据像移位寄存器一样依次穿过所有设备。始终 4 根线，但要求从设备同型号同配置、不能单独访问某一个、延迟随链长增加。典型用于级联 LED 驱动、多路 ADC 同步采样。

| 特性 | 独立 CS | 菊花链 |
|:---|:---|:---|
| 线数 | N+3 | 4 |
| 独立访问 | 可以 | 不行，整链一起移 |
| 设备类型 | 可混合 | 通常要求同型号 |
| 典型应用 | 通用多外设 | 级联 LED 驱动、同步 ADC |

---

## <span class="blue"> 电气特性与信号完整性

### 推挽输出（与 I2C 开漏的本质区别）

| 特性 | SPI（推挽） | I2C（开漏 + 上拉） |
|:---|:---|:---|
| 驱动结构 | PMOS+NMOS 主动拉高拉低 | 只能拉低，靠上拉电阻回高 |
| 上拉电阻 | 不需要 | 必须 |
| 边沿速度 | 快，驱动强 | 慢，受 RC 限制 |
| 总线冲突 | 有短路风险（两推挽互怼） | 无（线与） |
| 量级 | 数十至上百 MHz | ≤3.4 MHz |

推挽带来速度，也带来约束：两个主设备的 SPI 口不能直接并联；MISO 线上多从设备时必须保证未选中的从设备 MISO 为高阻（CS 拉高即释放——选从设备时确认这一点，个别老器件 MISO 不释放，需要外接三态缓冲）。

### 走线与速率（经验值）

| 速率 | 建议走线长度 | 要点 |
|:---|:---|:---|
| <1 MHz | 数十厘米 | 几乎无约束 |
| 1~10 MHz | 10~30 cm | 缩短平行走线 |
| 10~25 MHz | 5~15 cm | 完整地平面 |
| 25~50 MHz | <10 cm | 串联端接 22~47 Ω |
| >50 MHz | <5 cm | 严格 SI 设计 |

> ⚠️ 杜邦线飞线别超过几 MHz。飞线电感电容大，高速下数据偶发出错、时好时坏——先把速率降到 1 MHz 验证，能稳定就是信号质量问题而非逻辑问题。

SPI 异常的第一排查手段：**逻辑分析仪抓 SCLK 空闲电平和数据边沿**——先确认主从模式一致（最高频故障），再看 CS 是否在完整事务期间保持有效；无仪器时用回环测试（MOSI 短接 MISO 跑 `spidev_test -D /dev/spidevX.Y`，工具细节见 B-B.4.4）验证控制器本身收发正常，把问题二分到"控制器侧/从设备侧"。

---

## <span class="blue"> 方案对比（Trade-off）

| 维度 | 评价 |
|------|------|
| SPI vs I2C | 全双工高速、无寻址开销；代价是线多、无 ACK 无流控、多从设备吃 CS |
| 独立 CS | 灵活独立访问；代价是 N+3 根线 |
| 菊花链 | 恒 4 线；代价是同型号约束、无独立访问、延迟叠加 |
| 提速 | 吞吐线性增长；代价是走线/端接/地平面要求陡增 |
| GPIO 模拟 | 不占控制器、任意引脚；代价是速率低、CPU 占用高 |

---

## <span class="blue"> 常见陷阱

> ⚠️ 主从模式不一致。SPI 无 ACK，配错模式数据静默错乱，不报错。第一核对项永远是 CPOL/CPHA。

> ⚠️ 位序（MSB/LSB first）不一致。症状与模式错误相似：数据"收到了但全不对"。模式正确后再核位序。

> ⚠️ CS 在事务中途被拉高。多字节命令被从设备拆成两笔，状态机错位。一次完整事务内 CS 必须持续有效。

> ⚠️ 多从设备时某从机 MISO 不释放。未选中的设备仍驱动 MISO，总线互相打架。确认所有从机 CS 拉高后 MISO 高阻，必要时加三态缓冲。

> ⚠️ 飞线跑高速。偶发错误、温度相关、时好时坏——先降速到 1 MHz 二分定位。

---

## <span class="blue"> 动手练习

1. **模式辨认**：给定四种 SCLK 空闲电平 + 数据边沿组合，说出对应模式号；再反向，由模式号画出 SCLK 与数据位的相对关系。
2. **手册提取**：找一颗 SPI 器件手册（W25Qxx、ADXL345 均可），定位其模式要求、SCLK 上限、位序三项，记录出处页码。
3. **回环验证**：开发板上把 SPI 的 MOSI 与 MISO 短接，运行 `spidev_test -D /dev/spidevX.Y`，发送数据应原样收回——以此验证控制器与 pinctrl 配置正确。
4. **无硬件后备**：画 Mode 0 与 Mode 3 各 8 个时钟的完整波形（CS/SCLK/MOSI 三条线），标注采样点与数据变化点，互查是否符合"采样点落在数据稳定区"。

---

## <span class="blue"> 本节总结

| 自查项 | 确认标准 |
|--------|----------|
| 四线 | SCLK/MOSI/MISO/CS 方向与功能；全双工含义 |
| 四种模式 | CPOL/CPHA 定义；Mode 0/3 波形；主从必须一致 |
| 速率 | 由最苛刻一方决定；先半速验证再提速 |
| 拓扑 | 独立 CS vs 菊花链的取舍 |
| 电气 | 推挽、无上拉、MISO 高阻要求、走线经验值 |
| 排查锚点 | 先抓波形核模式，再回环二分控制器/从设备 |

---

## <span class="blue"> 配套资源

- **器件手册**：W25Qxx（Winbond）、ADXL345（ADI）SPI 章节
- **工具**：逻辑分析仪（SPI 解码）、`spidev_test`（内核 `tools/spi/`）
- **内核绑定**：`Documentation/devicetree/bindings/spi/rockchip,spi.yaml`

---

## <span class="blue"> 下一步

物理层之后是协议细节：**B-B.4.2 SPI 协议层与时序**——数据手册时序参数（建立/保持时间、CLK-to-Q）怎么读、典型读写事务的帧结构、Dual/Quad SPI 的扩展机制。随后 **B-B.4.3** 讲 Linux 驱动框架，**B-B.4.5 实战篇**用 W25Qxx NOR Flash 走通全链路。

> 💡 螺旋衔接：本篇推挽/开漏对比正是 B-B.3.1 I2C 物理层的镜像；CS 用 GPIO 扩展时的配置方法在 B-B.2.1；SPI 控制器挂在 APB 总线上的位置回看 B-A.1.1 的地址映射。
