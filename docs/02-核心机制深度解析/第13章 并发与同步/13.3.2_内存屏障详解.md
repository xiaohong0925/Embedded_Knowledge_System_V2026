# 13.3.2 内存屏障详解

> 所属章节：第13章 并发与同步 > 13.3 原子操作与内存屏障
>
> 难度：[E→M] | 预计阅读时间：15分钟

## <span class="blue"> 本节导读

本节覆盖：指令重排的两层来源（编译器优化与 CPU 乱序执行）、Linux 三种 SMP 内存屏障（`smp_mb()`/`smp_rmb()`/`smp_wmb()`）在 ARM64 上的真实指令映射（v6.6 源码核对）、Flag+Data 经典发布模式，以及过度使用屏障的性能代价。

## <span class="blue"> 从编译器重排到CPU乱序 [E]

要理解内存屏障，得先理解"为什么指令会不按顺序执行"。这里有两层来源。

### 编译器重排

编译器优化代码时，认为你的程序是单线程的。它看到两条没有数据依赖的内存操作，就可能交换它们的顺序来隐藏延迟、提高流水线利用率。

看这段代码：

```c
/* 全局变量 */
int data = 0;
int flag = 0;

void writer(void)
{
    data = 42;      /* 写数据 */
    flag = 1;       /* 设置标志，通知读者数据已就绪 */
}
```

编译器一看：`data`和`flag`之间没有依赖关系，那把`flag = 1`提到前面去也没事，还能减少一次缓存未命中。于是优化后变成：

```asm
/* 编译器重排后的伪代码 */
store flag, #1      /* 先写flag！ */
store data, #42     /* 后写data */
```

多核场景下，另一个CPU可能在`flag = 1`之后立刻读取`data`，结果拿到的是0——数据还没准备好。这种Bug在生产环境中极难复现，因为它高度依赖时序和缓存状态。

### CPU乱序执行

就算编译器老老实实按顺序生成代码，现代CPU的硬件也会乱序执行。以ARM64为例：

- **Store Buffer**：CPU不需要等写操作真正到达内存，就可以继续执行后续指令
- **Invalidate Queue**：收到缓存失效请求后，CPU可以先应答"已失效"，稍后再真正处理
- **Load/Store Speculation**：CPU可能预测性地提前执行读操作

<!--
【待补图·优先级★】文件路径：images/13.3.2-存储缓冲与失效队列结构.png

生图说明（整段复制给生图 AI，中文标注）：
画面主题：现代 CPU 写路径上的两个"重排元凶"——Store Buffer 与 Invalidate Queue 的硬件结构示意。
构图：横向分层技术框图，数据流向自上而下。顶部左右并排两个 CPU 核方框（标注"CPU0""CPU1"），每个核内部自上而下三个子模块：①执行单元（标注"流水线/乱序执行"）②Store Buffer（画成 4 格队列，标注"写操作先入此队列，不直达缓存"）③私有 L1 Cache（标注"64 字节缓存行为单位"）。每个核的 L1 Cache 右侧挂一个小队列框（标注"Invalidate Queue：收到失效请求先应答、后真正处理"）。两个核下方是一条横贯的宽带（标注"缓存一致性互联（总线/MESI 事务）"），最底部一个宽方块（标注"主内存 DRAM"）。用箭头标出三条关键路径：a) CPU0 执行单元→Store Buffer（旁注"写被缓冲，CPU 继续执行后续指令"）；b) Store Buffer→L1 Cache（旁注"稍后刷入"）；c) 互联→CPU1 的 Invalidate Queue（旁注"先回 ACK，失效延后生效"）。
风格：扁平矢量技术插图，白底，蓝灰主色 + 橙红色高亮两条缓冲队列，细线框，无渐变无阴影，A4 横版比例 16:9。
禁用：照片质感、3D 效果、卡通人物、英文标注。
注意：生图 AI 对中文文字标注容易出错，生成后需人工核对所有文字，必要时后期补字。

图片生成完成后删除本注释块，替换为：
![Store Buffer与Invalidate Queue结构：CPU核内执行单元→Store Buffer→L1 Cache，失效请求进Invalidate Queue先应答后处理，两核经缓存一致性互联访问主内存](images/13.3.2-存储缓冲与失效队列结构.png)
-->

> 💡 ARM64的内存模型是"弱序"（weakly ordered），允许大量重排；x86则是"强序"（TSO），允许的重排类型少很多。这就是为什么同一段无锁代码在x86上能跑、到ARM上就崩溃的典型原因。

两者对比一目了然：

| 维度 | 编译器重排 | CPU乱序执行 |
|:---|:---|:---|
| **来源** | 编译器优化阶段（-O2及以上） | CPU硬件执行阶段 |
| **影响范围** | 同一编译单元内的指令 | 跨核心的内存可见性 |
| **检测方法** | 对比C代码与汇编输出（`objdump -d`） | 仅在高并发+弱序CPU上偶发 |
| **解决方式** | `barrier()`编译器屏障 | `smp_mb()`等运行时内存屏障 |
| **是否可单独控制** | 可以，`barrier()`不涉及CPU指令 | 必须配合编译器屏障使用 |

> ⚠️ 很多人只关心CPU乱序，却忽略了编译器重排。实际上，你插了`smp_mb()`就同时解决了两层问题——它既是编译器屏障也是CPU屏障。

## <span class="blue"> 三种内存屏障与ARM64映射 [E→M]

Linux内核提供了三种主要的SMP内存屏障，对应不同的重排限制。

### 屏障函数速查

ARM64 上的真实映射来自 arch/arm64/include/asm/barrier.h（v6.6 源码核对）：

```c
#define __smp_mb()	dmb(ish)
#define __smp_rmb()	dmb(ishld)
#define __smp_wmb()	dmb(ishst)
```

| 函数 | 功能 | ARM64指令 | 使用场景 | 性能影响 |
|:---|:---|:---|:---|:---|
| `smp_mb()` | 全屏障，前后读写均不可重排 | `DMB ISH`（Inner Shareable，Full） | 需要严格序保证的关键路径 | 最高，需等待所有操作完成 |
| `smp_rmb()` | 读屏障，前后读操作不可重排 | `DMB ISHLD`（Load） | 读者检查flag后读取数据 | 中等，仅序化读操作 |
| `smp_wmb()` | 写屏障，前后写操作不可重排 | `DMB ISHST`（Store） | 写者写入数据后设置flag | 最低，仅序化写操作 |

> 💡 `ISH` = Inner Shareable，表示屏障影响同一个inner shareable domain内的所有CPU核心。这是Linux内核SMP场景的默认范围。

### mermaid图：屏障前后的重排限制

```mermaid
graph LR
    subgraph "无屏障：允许重排"
        A[Op A] --> B[Op B]
        C[Op C] --> D[Op D]
        B -.->|"可能重排到前面"| A
        D -.->|"可能重排到前面"| C
    end

    subgraph "smp_mb() 全屏障"
        E[Op E] -->|"必须完成"| F["🚧 smp_mb() 🚧"]
        F -->|"Op G/H 在此之后"| G[Op G]
        F --> H[Op H]
        style F fill:#ff6b6b,color:#fff
    end

    subgraph "smp_wmb() 写屏障"
        I[写数据] -->|"必须完成"| J["🚧 smp_wmb() 🚧"]
        J --> K[写flag]
        style J fill:#4ecdc4,color:#fff
    end

    subgraph "smp_rmb() 读屏障"
        L[读flag] -->|"必须完成"| M["🚧 smp_rmb() 🚧"]
        M --> N[读数据]
        style M fill:#45b7d1,color:#fff
    end
```

## <span class="blue"> 典型模式：Flag + Data 双缓冲 [M]

这是内核中最经典的内存屏障使用场景。一个CPU写入数据后通过flag通知另一个CPU。

### 完整代码示例

```c
/* 全局共享变量 */
static int   shared_data;    /* 要传递的数据 */
static int   data_ready;     /* 标志位：0=未就绪, 1=已就绪 */

/*========== 写者（CPU 0）==========*/
void writer_submit(int value)
{
    /* 步骤1：先写入实际数据 */
    shared_data = value;          /* 数据必须先到达内存 */

    /* 步骤2：写屏障 —— 确保shared_data在data_ready之前 */
    smp_wmb();

    /* 步骤3：设置标志，通知读者 */
    data_ready = 1;               /* 读者看到这个就知道数据有效了 */
}

/*========== 读者（CPU 1）==========*/
int reader_poll(void)
{
    int ready;

    /* 步骤1：读取标志 */
    ready = data_ready;

    /* 步骤2：读屏障 —— 确保flag读在data读之前完成 */
    smp_rmb();

    /* 步骤3：只有flag为1时，data才是有效的 */
    if (ready) {
        /* 此时读取shared_data，一定能看到写者写入的值 */
        return shared_data;       /* 安全读取 */
    }
    return -EAGAIN;               /* 数据还没准备好 */
}
```

这个模式的精髓在于：写者用`smp_wmb()`保证"数据先于标志"，读者用`smp_rmb()`保证"标志先于数据"。两者配合，就形成了一个安全的**发布/订阅**（Publish/Subscribe）通道。

> 💡 RCU（Read-Copy-Update）的指针发布就是这种模式的高级版本——先构造好新数据结构，再`smp_wmb()`，最后原子替换指针。读者侧则反过来：先读指针，再`smp_rmb()`，最后解引用。

## <span class="blue"> 过度屏障与性能代价 [M]

内存屏障不是免费的午餐。每次调用`smp_mb()`，CPU都要做额外工作来确保序。

### 性能影响对比

| 操作 | 大致开销（周期） | 说明 |
|:---|:---|:---|
| 普通内存访问 | 3~100+ | 取决于缓存命中情况 |
| `smp_rmb()` | ~10-20 | 仅需排空读队列 |
| `smp_wmb()` | ~15-25 | 需排空Store Buffer |
| `smp_mb()` | ~30-60+ | 需排空所有读写队列，可能触发缓存同步 |
| 原子操作（带返回值） | 15-30 | 隐含全序语义（见下方说明） |

> 🔴 在热路径上滥用`smp_mb()`可能导致性能暴跌。原则是：**用最弱的屏障满足语义需求，只在真正需要序保证的地方使用。**

> 💡 澄清一个常见误解：Linux 的 `atomic_t` 接口**并不**默认带顺序一致性。按 `Documentation/atomic_ops.rst` 的口径：**无返回值的 RMW 操作**（`atomic_inc()`、`atomic_add()` 等）**不提供任何内存序保证**；**带返回值的**（`atomic_inc_return()`、`atomic_add_return()`、`atomic_dec_and_test()` 等）才是全序（隐含 `smp_mb()` 前后的完整屏障）。中间档位还有 `_relaxed` / `_acquire` / `_release` 后缀变体（如 `atomic_inc_return_acquire()`）供精细控制。所以"用了原子操作就不用管内存序"是错的——只有 `_return` 系和带序后缀变体给你序保证。

## <span class="blue"> 本节总结

| 要点 | 内容 |
|:---|:---|
| **为什么需要内存屏障** | 编译器重排 + CPU乱序执行，多核下导致内存操作可见性失控 |
| **三种屏障** | `smp_mb()`全屏障、`smp_rmb()`读屏障、`smp_wmb()`写屏障，限制重排范围递增递减 |
| **ARM64映射** | `DMB ISH` / `DMB ISHLD` / `DMB ISHST`（v6.6 `asm/barrier.h` 核对），理解ISH的共享域范围 |
| **核心模式** | Flag+Data发布/订阅：写者先写数据→`smp_wmb()`→写flag；读者先读flag→`smp_rmb()`→读数据 |
| **原子操作的序** | 无返回值 RMW 无序保证；`_return` 系全序；`_acquire/_release/_relaxed` 精细控制 |
| **使用原则** | 能不用就不用，需要时选最弱的，热路径尤其谨慎 |

## <span class="blue"> 下一步

内存屏障解决的是"指令重排导致的可见性问题"，但它有一个前提：编译器不能把你对共享变量的访问"优化掉"——比如发现你读了一个变量但"没用"，就直接删掉这行代码，那你精心设计的屏障就白费了。

> **13.3.3 READ_ONCE与WRITE_ONCE**

## <span class="blue"> 配套资源

| 资源 | 说明 |
|:---|:---|
| 代码清单 | `writer_submit()` / `reader_poll()` —— 完整Flag+Data模式 |
| 图示 | mermaid屏障重排限制图 —— 三种屏障的约束范围 |
| 速查表 | 三种屏障函数与ARM64指令映射表（v6.6 `arch/arm64/include/asm/barrier.h`） |
| 内核文档 | `Documentation/memory-barriers.txt`（Linux内核源码） |
| 内核文档 | `Documentation/atomic_ops.rst`（原子操作的内存序语义） |
| ARM手册 | ARM Architecture Reference Manual，DMB指令章节 |
