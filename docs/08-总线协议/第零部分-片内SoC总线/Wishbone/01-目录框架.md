# Wishbone 总线协议 **[B→I]**

> <span class="badge-b">B</span> → <span class="badge-i">I</span>
> 所属：08-总线协议 / 第零部分-片内 SoC 总线
> 定位：最轻量、最开源的片上总线，FPGA 原型验证和教学的首选

**文件规划：**
```
Wishbone/
├── 01-Wishbone基础认知与架构.md          [B→I]
├── 02-Wishbone传输时序与变体.md           [I]
└── 03-Wishbone实战与FPGA应用.md          [I→E]
```

---

## 01-Wishbone基础认知与架构.md **[B→I]**

> <span class="badge-b">B</span> → <span class="badge-i">I</span>

### <strong>Wishbone 核心定义与价值</strong>

<span class="badge-b">B</span>

- 什么是 Wishbone
  - OpenCores 组织推出的开源片上总线标准，2002 年发布 B.3 版
  - 设计目标：简单到可以在课堂上手写 RTL，开放到没有授权费用
  - 不是商业标准，没有 ARM/SiFive 这样的大厂商背书，但生态活跃
- 为什么还需要 Wishbone（不是有 AMBA 和 TileLink 了吗？）
  - FPGA 友好：信号少、时序简单，综合工具容易优化
  - 教学友好：本科生能在一节课里理解并写出 Wishbone Master/Slave
  - 开源惯性：OpenCores 上有数百个 Wishbone IP 核（UART、SDRAM、VGA），即插即用
  - <span class="blue">Wishbone 是总线世界里的乐高积木，简单、开放、适合动手玩。</span>
- 典型应用场景
  - 学术项目：FPGA 课程设计、毕业设计中的 SoC 原型
  - 开源处理器：OpenRISC、RISC-V 早期软核（如 PicoRV32）的默认总线
  - 工业原型：快速验证外设 IP，验证通过后再迁移到 AXI/TileLink

### <strong>Wishbone 与 AMBA/TileLink 的定位差异</strong>

<span class="badge-b">B</span>

| 维度 | Wishbone | AMBA | TileLink |
|------|----------|------|----------|
| 出身 | OpenCores 开源 | ARM 商业 | RISC-V/SiFive |
| 协议页数 | ~30 页 | ~300 页 | ~100 页 |
| 缓存一致性 | 不支持 | ACE/CHI | 原生支持 |
| 突发传输 | 可选（循环突发） | 原生支持 | 原生支持 |
| 典型频率 | FPGA 50-100MHz | ASIC GHz 级 | ASIC/FPGA 都支持 |

- 选择逻辑：FPGA 教学/原型 → Wishbone；商业 ASIC → AMBA；RISC-V ASIC → TileLink

### <strong>Wishbone 信号速览</strong>

<span class="badge-i">I</span>

- CLK_I / RST_I：时钟与复位
- ADR_O / ADR_I：地址总线（可变位宽，通常 8-32bit）
- DAT_O / DAT_I：数据总线（双向，通常 8/16/32bit）
- WE_O：读写方向（1=写，0=读）
- SEL_O：字节选通（类似 AXI WSTRB）
- STB_O：选通信号（Transaction 有效）
- CYC_O：总线周期（burst 期间保持拉高）
- ACK_I：Slave 确认（单周期握手完成）
- ERR_I：错误响应（可选）
- RTY_I：重试响应（可选）
- 信号总数：约 10-15 根，远少于 AXI 的 50+

---

## 02-Wishbone传输时序与变体.md **[I]**

> <span class="badge-i">I</span>

### <strong>标准传输时序</strong>

<span class="badge-i">I</span>

- Wishbone 只有一种传输模式：单周期握手（类似 APB 的简化版）
- 标准周期：
  - T1：Master 放 ADR/DAT/WE/SEL/STB/CYC，Slave 采样
  - T2：Slave 返回 ACK + DAT（读时），Master 看到 ACK 后撤 STB
- 等待周期：Slave 拉低 ACK，Master 保持所有信号，直到 ACK 拉高
- 对比 APB：Wishbone 和 APB 都是单周期握手，但 Wishbone 信号更少、CYC 支持 burst

### <strong>突发传输变体</strong>

<span class="badge-i">I</span>

- 单次传输：CYC=1，STB=1，ACK=1，完成
- 块传输（Block Transfer）：CYC 保持拉高，STB 每个周期重新拉高，连续多个 beat
- 循环突发（Cycle Burst）：地址自动递增，类似 AXI INCR burst，但 Master 自己算地址
- Wishbone 规范不强制突发，但推荐 Master 支持递增地址模式

### <strong> Wishbone 变体：经典 vs 流水线 vs 交叉开关</strong>

<span class="badge-i">I</span>

- 经典 Wishbone：上述标准时序，最简单
- 流水线 Wishbone：地址和数据周期分离，类似 AXI 的单方向流水，增加带宽
- 交叉开关（Crossbar）：多主多从，通过仲裁器实现，OpenCores 有开源实现
- 选择：简单外设用经典，高性能通路用流水线，多核系统用交叉开关

---

## 03-Wishbone实战与FPGA应用.md **[I→E]**

> <span class="badge-i">I</span> → <span class="badge-e">E</span>

### <strong>实战 1：Wishbone 简单外设（GPIO 控制器）RTL 设计</strong>

<span class="badge-i">I</span>

- 场景：FPGA 上实现 32 路 GPIO，通过 Wishbone 接口配置方向/数据
- RTL 结构：
  - 地址译码：ADR[3:2] 选择寄存器（方向寄存器 / 数据寄存器 / 置位寄存器 / 清零寄存器）
  - 写操作：WE=1 && STB=1 && ACK=1 时，DAT_I 写入对应寄存器
  - 读操作：WE=0 时，DAT_O 返回寄存器值，ACK 拉高
- 验证：用 Verilator 仿真，写 testbench 模拟 Master 发起读写

### <strong>实战 2：多外设 Wishbone 地址映射与仲裁器设计</strong>

<span class="badge-e">E</span>

- 场景：FPGA SoC 有 UART、SPI、Timer、GPIO，共用一条 Wishbone 总线
- 地址分配：
  - 0x0000-0x0FFF：UART
  - 0x1000-0x1FFF：SPI
  - 0x2000-0x2FFF：Timer
  - 0x3000-0x3FFF：GPIO
- 仲裁器：固定优先级（CPU > DMA）或轮询，生成对应 Slave 的 STB 信号
- 时序注意：仲裁切换时插入 1 个空闲周期，避免地址冲突

### <strong>实战 3：Wishbone to AXI4 桥接器（FPGA 原型迁移路径）**[M]**</strong>

<span class="badge-m">M</span>

- 场景：FPGA 原型验证通过，要迁移到 ASIC，ASIC IP 只支持 AXI4
- 桥设计要点：
  - Wishbone 单次传输 → AXI4 单次 burst（LEN=0）
  - Wishbone 块传输 → AXI4 INCR burst（地址递增）
  - 没有缓存一致性：Wishbone 不支持，桥接时直接透传，一致性由上层保证
- 开源参考：ZipCPU 的 wbm2axisp 桥，可直接综合到 FPGA 验证

---

## 附：学习路径

| 读者 | 阅读路径 | 目标 |
|------|----------|------|
| **小白 (B)** | 01 全部 → 02 标准时序 | 理解 Wishbone 为什么存在，能看懂信号表 |
| **高手 (I→E)** | 02 变体 → 03 实战 1+2 | 能设计 Wishbone 外设 RTL，规划多外设地址映射 |
| **大师 (E→M)** | 03 实战 3（桥接器）→ 流水线 Wishbone 优化 | 能做 FPGA→ASIC 迁移，设计高性能 Wishbone 交叉开关 |

---

**自检：**
- [x] 3 小章，极简风格匹配协议定位
- [x] 3 个实战：GPIO RTL / 多外设仲裁 / FPGA→ASIC 桥
- [x] 与 AMBA/TileLink 对比只做生态定位说明
- [x] 每小节可展开 200-300 字段落
